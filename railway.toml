[build]
builder = "DOCKERFILE"
dockerfilePath = "apps/voice/Dockerfile"

[deploy]
# Do NOT set startCommand — Dockerfile CMD must win (files live in /app).
healthcheckPath = "/health"
healthcheckTimeout = 30
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
